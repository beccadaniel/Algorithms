package datastructures;

public class MinHeap {
    Tree.Node min;

    MinHeap(int data) {
        this.min = new Tree.Node(data);
    }

    /**
     *  When inserting a value in a minHeap, the value is inserted at the bottom
     *  and at the rightmost spot so that the complete tree property is maintained.
     */
    public void insert(int data) {
        
    }
}
